fun main() {
    //initialize variable
    var base:Int = 10
    var height:Int = 2
    var areaTriangle:Int = 0

    //process value
    areaTriangle = (base * height)/2

    //output value
    println("Area of triangle: $areaTriangle")

    //Assume value
    var a:Int = 2
    var b:Int = 1
    var c:Int = 0

    c = ((a*2) + (2*a*b) -(4 * (b+a)))/2
    println("Output value of c is $c")

}